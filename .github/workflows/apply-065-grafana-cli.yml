name: Apply installer 0.6.5 Grafana CLI path fix

on:
  push:
    branches:
      - fix/grafana-cli-path-0.6.5

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply patch helper
        run: python3 tools/apply_grafana_cli_path_065.py

      - name: Validate patch
        shell: bash
        run: |
          set -euo pipefail
          chmod +x tests/grafana-cli-path.sh
          bash -n scripts/install.sh
          bash -n scripts/lib/common.sh
          bash -n scripts/lib/distro-rocky.sh
          bash -n scripts/lib/distro-ubuntu.sh
          bash -n tests/grafana-cli-path.sh
          ./tests/platform-detection.sh
          ./tests/ubuntu-resolver-contract.sh
          ./tests/pipefail-safety.sh
          ./tests/lam-config-json.sh
          ./tests/grafana-provisioning-permissions.sh
          ./tests/grafana-cli-path.sh

      - name: Commit patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/install.sh scripts/lib/common.sh CHANGELOG.md tests/grafana-cli-path.sh
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "fix: detect Grafana CLI binary path"
          git push
